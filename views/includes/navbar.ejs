<nav class="navbar navbar-expand-lg bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a  class="navbar-brand d-flex align-items-center gap-2" href="/"><i class="fa-solid fa-compass"></i><span>Stay_hub</span></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
          <a class="nav-link" href="/listings">All Listings</a>&nbsp &nbsp &nbsp
          <% if (currentUser) { %>
            <a class="nav-link" href="/listings/new">Add New Listing</a>
          <% } %>
      <ul class="navbar-nav ms-auto">
        <% if (!currentUser) { %>
          <li class="nav-item"><a class="btn btn-outline-dark me-2" href="/signup">Sign Up</a></li>
          <li class="nav-item"><a class="btn btn-dark" href="/login">Login</a></li>
        <% } else { %>
          <li class="nav-item"><span class="me-3">Hi, <%= currentUser.username %></span></li>
          <li class="nav-item"><a class="btn btn-danger" href="/logout">Logout</a></li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>